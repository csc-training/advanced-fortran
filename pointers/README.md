## New pointer features and contiguous attribute

Compile and run the program `pointer.F90` to see how the new pointer
features work. Then fix `contiguous.F90` such that you get the
diagonal as intended.
